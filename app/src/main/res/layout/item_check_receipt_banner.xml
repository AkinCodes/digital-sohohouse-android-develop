<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/dp_16"
    android:background="@drawable/rounded_rect_border_radius4"
    android:padding="@dimen/dp_16">

    <TextView
        android:id="@+id/receipt_banner_primary"
        style="@style/Body.014.Narrow.Light"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Discount not applied" />

    <TextView
        android:id="@+id/receipt_banner_secondary"
        style="@style/Body.014"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/dp_4"
        app:layout_constraintTop_toBottomOf="@id/receipt_banner_primary"
        tools:text="Your discount couldn’t be redeemed as it is only valid if the tab is settled on the day and closed by you. Unfortunately is not possible to have a refund or retrospective discount." />

</androidx.constraintlayout.widget.ConstraintLayout>