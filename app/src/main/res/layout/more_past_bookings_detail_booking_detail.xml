<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="@dimen/dp_16"
    android:paddingEnd="@dimen/dp_16">


    <TextView
        android:id="@+id/detail_title"
        style="@style/body_02"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="@dimen/medium"
        android:textColor="@color/white"
        app:layout_constraintBottom_toBottomOf="@+id/detail_supporting"
        app:layout_constraintEnd_toStartOf="@+id/detail_supporting"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/detail_supporting"
        tools:text="Booking Status" />

    <TextView
        android:id="@+id/detail_supporting"
        style="@style/body_02"
        android:layout_width="wrap_content"
        android:textColor="@color/white"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/dp_12"
        android:paddingBottom="@dimen/dp_13"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="@string/explore_events_booking_details_confirmed_label" />

    <View
        android:id="@+id/divider"
        style="@style/Divider.Iron"
        android:layout_width="match_parent"
        app:layout_constraintTop_toBottomOf="@id/detail_supporting" />

</androidx.constraintlayout.widget.ConstraintLayout>