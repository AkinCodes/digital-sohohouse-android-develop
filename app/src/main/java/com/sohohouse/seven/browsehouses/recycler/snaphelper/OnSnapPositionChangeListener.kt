package com.sohohouse.seven.browsehouses.recycler.snaphelper

interface OnSnapPositionChangeListener {
    fun onSnapScrollPositionChange(position: Int)
    fun onSnapIdlePositionChange(position: Int)
}